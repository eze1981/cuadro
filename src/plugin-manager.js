
export default {
  init: () => {
    // console.log('plugin manager initialized!')
  },

  // install a new plug-in
  install: (pluginName) => {
    
    // 1. verify if plug-in is already installed

    // 2. download plug-in

    // 3. validate plug-in interface

    // 4. install plug-in

    // 5. register plug-in

  },

  // uninstall an existing plug-in
  uninstall: (pluginName) => {

  }

}